import React, { Fragment } from 'react';

const CardTotal = ({ children }, props) => {
    return (
        <div class="pure-u-1-4">
            {children}
        </div>
    )
}

export default CardTotal;